# ゆるゲージ revision prompts

## Review Fixes
- Metadata mismatch: align title and description with `進捗と返事`, not generic business honorifics.
- Text-in-image issue: verify every label is intentional, correct, and readable.
- Duplicate package issue: compare `了 / 済 / 決`, `中 / 調`, `見 / 確 / 返` before submission.

## Quality Fixes
- Visibility: increase label size, simplify sign, thicken outline.
- Difference clarity: adjust progress bar amount and status sign before changing labels.
- Spacing: keep outer shape large; transparent margin around 14px for content images.
- Metadata alignment: keep suggest tags focused on actual contents.

## Post-Release Fixes
- If `休` underperforms or weakens product fit, replace in release-002 with a stronger state such as `助` or `送`.
- If business search dominates but daily use is weak, create a schedule-focused derivative rather than overloading this release.

